const arr = ['Islam', 'Magomed', 'Islam', 'Alvi', 'Anzor', 'Ahmad', 'Arthur', 'Johny', 'Silver']
    for (let i = 0; i < arr.length; i++) {
        if (arr[i][0] === 'A') {
            console.log(arr[i])
        }
    }
