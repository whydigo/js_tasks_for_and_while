const arr = ['php', 'Java', 'C++', 'C#', 'JavaScript', 'TypeScript', 'Kotlin', 'Python', 'C', 'Swift']
let i = 0
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i])
    }
    i++
}